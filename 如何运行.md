# 螺旋机器人设计工具 - 使用说明

## 📦 环境配置

### 1. 安装 Conda

如果还没有安装 Conda，请下载安装 [Anaconda](https://www.anaconda.com/) 或 [Miniconda](https://docs.conda.io/en/latest/miniconda.html)。

### 2. 创建虚拟环境

```bash
conda env create -f environment.yml
```

这将创建名为 `bionic-hand` 的 conda 环境，并安装所有依赖。

## 🚀 运行程序（推荐方式）

### 使用 conda run 命令

```bash
conda run -n bionic-hand python design_software\design_software.py
```

**优点：**
- ✅ 无需激活环境
- ✅ 无需管理员权限
- ✅ 无需初始化配置
- ✅ 一条命令搞定

这是最简单、最可靠的运行方式！

### VSCode 中运行

1. **选择 conda 环境的 Python 解释器**
   - 按 `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)
   - 输入 "Python: Select Interpreter"
   - 选择 `bionic-hand` 环境的 Python

2. **按 F5 运行**
   - VSCode 会自动使用选定的 conda 环境

## � 其他运行方式

### 方式2：在 VSCode 中运行

1. **选择 Python 解释器**
   - 按 `Ctrl+Shift+P` (Mac: `Cmd+Shift+P`)
   - 输入 "Python: Select Interpreter"
   - 选择 `bionic-hand` 环境的 Python

2. **运行程序**
   - 打开 `design_software/design_software.py`
   - 按 `F5` 运行

### 方式3：手动激活环境（高级用户）

如果你的 shell 已经配置好 conda，也可以手动激活：

```bash
conda activate bionic-hand
python design_software\design_software.py
conda deactivate  # 用完后退出
```

**注意：** PowerShell 需要先运行 `conda init powershell` 并重启才能使用 `conda activate`。如果遇到问题，直接使用方式1（conda run）即可。

## 📝 常见问题

### ⚠️ 为什么直接运行 python 会报错？

如果你直接运行 `python design_software\design_software.py`，会看到：

```
ModuleNotFoundError: No module named 'PySide6'
```

**原因：** 系统使用的是默认 Python，而不是 conda 环境中的 Python。

**解决方法：** 使用 `conda run -n bionic-hand` 命令

## 📦 已安装的依赖包

- Python 3.11
- PySide6 6.10.2 (Qt6 GUI框架)
- matplotlib 3.10.8 (2D绘图)
- cadquery 2.4.0 (CAD建模)
- numpy 1.26.4 (数值计算)

## 🆘 常见问题

### Q: 每次都要输入长命令很麻烦？
A: 可以创建一个简短的别名或脚本。例如在 PowerShell 中：
```powershell
function Run-BionicHand { conda run -n bionic-hand python design_software\design_software.py }
Set-Alias run Run-BionicHand
# 之后只需输入: run
```

### Q: 如何查看所有环境？
A: 
```bash
conda env list
```

### Q: 删除环境？
A: 
```bash
conda env remove -n bionic-hand
```

### Q: 重新创建环境？
A: 
```bash
conda env remove -n bionic-hand
conda env create -f environment.yml
```

### Q: 更新依赖包？
A: 激活环境后：
```bash
conda update --all
# 或更新特定包
pip install --upgrade PySide6 matplotlib cadquery
```

---

## 🎉 快速总结

记住这两个命令就够了：

```bash
# 1. 创建环境（仅第一次）
conda env create -f environment.yml

# 2. 运行程序（每次使用）
conda run -n bionic-hand python design_software\design_software.py
```

简单、直接、不需要任何额外配置！

祝使用愉快！
